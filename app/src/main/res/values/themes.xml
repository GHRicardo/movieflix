<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MovieFlix" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MainActivity" parent="Theme.MovieFlix">
        <item name="colorPrimary">@color/toolbar_color</item>
    </style>

    <style name="ProgressBar">
        <item name="android:layout_height">@dimen/prg_size</item>
        <item name="android:layout_width">@dimen/prg_size</item>
        <item name="android:indeterminateTint">@color/white</item>
    </style>

    <style name="TxtMovieDetail">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_marginStart">@dimen/app_margin_se</item>
        <item name="android:layout_marginEnd">@dimen/app_margin_se</item>
    </style>

    <style name="TxtError" parent="TxtMovieDetail">
        <item name="android:gravity">center</item>
        <item name="android:visibility">gone</item>
        <item name="android:textSize">@dimen/search_txt_no_data_textsize</item>
    </style>

    <style name="ImgBack">
        <item name="android:layout_width">@dimen/back_size</item>
        <item name="android:layout_height">@dimen/back_size</item>
        <item name="android:layout_marginStart">@dimen/back_margin_s</item>
        <item name="android:layout_marginEnd">@dimen/back_margin_e</item>
        <item name="android:src">@drawable/outline_arrow_back_white_24</item>
    </style>

    <style name="ImgSearch">
        <item name="android:layout_width">@dimen/search_size</item>
        <item name="android:layout_height">@dimen/search_size</item>
        <item name="android:layout_marginStart">@dimen/search_margin_s</item>
        <item name="android:layout_marginEnd">@dimen/search_margin_e</item>
        <item name="android:src">@drawable/outline_search_white_24</item>
    </style>
</resources>